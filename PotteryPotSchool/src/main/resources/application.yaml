spring:
  application:
    name: PotteryPotSchool

  datasource:
    url: jdbc:postgresql://localhost:5432/potterypotschooldb
    username: postgres
    password: WEyfvb56.

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080